on: push
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v2
    
    - name: 📂 Sync files
      uses: ftsomuf
      with:
        server: ftp.cluster028.hosting.ovh.net
        username: ftsomuf
        password: Multilang2022
